---
import BaseLayout from './BaseLayout.astro'

interface Props {
  title?: string
}

const { title = 'Puck Editor' } = Astro.props
---

<BaseLayout title={title}>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@puckeditor/core/puck.css" />
  <slot />
</BaseLayout>
